## Запустите контейнеры:
- `Terminal`
  - docker-compose up --build
  - docker-compose up --build -d

## В БД сразу будет пользователь:
  - user_name: admin
  - password: admin

## Управление пользователями и билетами через админ панель.